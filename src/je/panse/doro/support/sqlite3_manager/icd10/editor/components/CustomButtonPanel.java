package je.panse.doro.support.sqlite3_manager.icd10.editor.components;

public class CustomButtonPanel extends JPanel {
    public CustomButtonPanel(ActionListener listener) {
        createRoundedGradientButton("Add New", Color.BLUE, listener);
        // Other button creations
    }
    
    private JButton createRoundedGradientButton(String text, Color baseColor, ActionListener listener) {
        // Button creation logic
    }
}
